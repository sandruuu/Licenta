apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: trustcloud
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/part-of: trustcloud
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
