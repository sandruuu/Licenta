apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-pdp-to-redis
  namespace: trustcloud
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: redis
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: pdp
      ports:
        - protocol: TCP
          port: 6379
